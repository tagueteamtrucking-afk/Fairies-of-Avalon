name: Deploy Worker - hello (debug)

on:
  workflow_dispatch: {}
  # push:
  #   branches: [ main ]
  #   paths:
  #     - "cloudflare/worker-hello.js"
  #     - ".github/workflows/hello-worker.yml"

permissions:
  contents: read

concurrency:
  group: hello-worker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Wrangler deploy
        env:
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          WORKER_NAME: hello-worker
        run: |
          npm i -g wrangler
          wrangler deploy cloudflare/worker-hello.js --name $WORKER_NAME
