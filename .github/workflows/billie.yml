name: Billie — LLM
on: { workflow_dispatch: {} }
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Billie
        env: { OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}, OPENAI_MODEL: ${{ vars.OPENAI_MODEL }} }
        shell: pwsh
        run: pwsh -File scripts/Agents-LLM.ps1 -Agent "Billie" -Count 3 -Task "outline monetization ideas and safe revenue reporting (no sensitive content)"
      - name: Commit output
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pages/apps/billie/
          if git diff --cached --quiet; then echo "No changes"; else git commit -m "feat(billie): updates" && git push; fi
