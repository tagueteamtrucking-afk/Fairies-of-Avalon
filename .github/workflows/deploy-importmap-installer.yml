name: Deploy Worker - importmap installer

on:
  workflow_dispatch: {}
  # push:
  #   branches: [ main ]
  #   paths:
  #     - "cloudflare/worker-importmap.js"
  #     - ".github/workflows/importmap-installer.yml"

permissions:
  contents: read

concurrency:
  group: importmap-installer-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Wrangler deploy
        env:
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          WORKER_NAME: importmap-installer
        run: |
          npm i -g wrangler
          wrangler deploy cloudflare/worker-importmap.js --name $WORKER_NAME
