name: Deploy Worker - hello (v4)

on:
  workflow_dispatch: {}
  # push:
  #   branches: [ main ]
  #   paths:
  #     - "cloudflare/worker-hello-v4.js"
  #     - ".github/workflows/hello-v4.yml"

permissions:
  contents: read

concurrency:
  group: hello-v4-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Wrangler deploy
        env:
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          WORKER_NAME: hello-v4
        run: |
          npm i -g wrangler
          wrangler deploy cloudflare/worker-hello-v4.js --name $WORKER_NAME
