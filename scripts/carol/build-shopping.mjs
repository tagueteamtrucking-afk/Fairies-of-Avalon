import fs from 'node:fs'; import path from 'node:path'; const dir=path.join('pages','apps','carol'); const outS=path.join(dir,'shopping-quantized.json'); const outR=path.join(dir,'plan-report.json'); let plan={}; try{plan=JSON.parse(fs.readFileSync(path.join(dir,'plan_latest.json'),'utf8'))}catch{plan={warning:'plan_latest.json missing'}}; const shopping={generated:new Date().toISOString(),items:[]}; const report={generated:new Date().toISOString(),notes:['Placeholder report']}; fs.writeFileSync(outS,JSON.stringify(shopping,null,2)); fs.writeFileSync(outR,JSON.stringify(report,null,2)); console.log('Wrote shopping-quantized.json and plan-report.json');