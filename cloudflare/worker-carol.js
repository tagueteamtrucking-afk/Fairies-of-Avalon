export default { async fetch(req, env){ const url=new URL(req.url); if(req.method!=='POST') return new Response('POST required',{status:405}); const body=await req.json().catch(()=>null); if(!body) return new Response('bad json',{status:400}); const type=body.type; const data=body.data||{}; const path=type==='prefs'?'pages/apps/carol/profile/preferences.json':type==='rating'?'pages/apps/carol/ratings.json':null; if(!path) return new Response('bad type',{status:400}); const owner=env.GITHUB_OWNER, repo=env.GITHUB_REPO, token=env.GITHUB_TOKEN; const api='https://api.github.com'; const repoRes=await fetch(`${api}/repos/${owner}/${repo}`,{headers:{Authorization:`Bearer ${token}`}}); const repoJson=await repoRes.json(); const base=repoJson.default_branch||'main'; const refRes=await fetch(`${api}/repos/${owner}/${repo}/git/ref/heads/${base}`,{headers:{Authorization:`Bearer ${token}`}}); const refJson=await refRes.json(); const baseSha=refJson.object.sha; const branch=`ci/carol-${type}-${Date.now()}`; await fetch(`${api}/repos/${owner}/${repo}/git/refs`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({ref:`refs/heads/${branch}`,sha:baseSha})}); const content=btoa(unescape(encodeURIComponent(JSON.stringify(data,null,2)))); const blob=await fetch(`${api}/repos/${owner}/${repo}/git/blobs`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({content,encoding:'base64'})}).then(r=>r.json()); const tree=await fetch(`${api}/repos/${owner}/${repo}/git/trees/${baseSha}`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.json()); const newTree=await fetch(`${api}/repos/${owner}/${repo}/git/trees`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({base_tree:tree.sha,tree:[{path,mode:'100644',type:'blob',sha:blob.sha}]})}).then(r=>r.json()); const commit=await fetch(`${api}/repos/${owner}/${repo}/git/commits`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({message:`carol: ${type} update`,tree:newTree.sha,parents:[baseSha]})}).then(r=>r.json()); await fetch(`${api}/repos/${owner}/${repo}/git/refs/heads/${branch}`,{method:'PATCH',headers:{Authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({sha:commit.sha,force:true})}); const pr=await fetch(`${api}/repos/${owner}/${repo}/pulls`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({title:`carol: ${type} update`,head:branch,base:base,body:`Automated update for Carol ${type}.`})}).then(r=>r.json()); return new Response(JSON.stringify({ok:true,pr:pr.html_url}),{headers:{'content-type':'application/json'}}); } }