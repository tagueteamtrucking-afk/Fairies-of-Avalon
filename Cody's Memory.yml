meta:
  project: Avalon
  file_name: "Cody's Memory.yaml"
  version: "1.7.6"
  last_updated: "2025-10-07T00:34:19Z"
  author: "Cody (GPT-5 Pro)"
  notes: >
    Patch v1.7.6 — Alexandria DM pack + sweeper for ?v= redirects;
    workflows-all shipped (flat); Overseers inherit all assistants' abilities/permissions.

governance:
  overseers:
    - id: rey_czar
      name: "Rey Czar"
      all_permissions: true
      inherits_all_assistants: true
    - id: white_star
      name: "White Star"
      all_permissions: true
      inherits_all_assistants: true

alexandria:
  dm_features:
    - "World pack: magic sources/costs/limits, pantheon, cultures, factions, laws"
    - "Random tables: hooks, NPCs, local events"
    - "Session seeds + house rules"
    - "Novelization outline from sessions"
  files:
    root: "pages/apps/alexandria/dm"
    index: "pages/apps/alexandria/dm/index.json"
    pattern: "pages/apps/alexandria/dm/<id>.json"
  workflows:
    - ".github/workflows/alexandria-dm.yml"
  scripts:
    - "scripts/Alexandria-DM-LLM.ps1"
    - "scripts/Alexandria-Validate.ps1"

site_hygiene:
  sweeper:
    script: "scripts/VersionParam-Sweeper.ps1"
    workflow: ".github/workflows/repo-sweeper.yml"
    purpose: "Detect and neutralize '?v=' redirect logic to stop loop"

defaults:
  openai_tts: true
  openai_model: "gpt-4.1"
