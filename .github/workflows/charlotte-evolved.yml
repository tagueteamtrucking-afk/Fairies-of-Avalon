name: Charlotte (evolved) — watch relay + compliance
on:
  push:
    branches: [ main ]
    paths:
      - "pages/apps/charlotte/**"
      - ".github/workflows/charlotte-evolved.yml"
  workflow_dispatch: {}
permissions: { contents: read }
concurrency: { group: charlotte-evolved-${ github.ref }, cancel-in-progress: true }
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Charlotte (evolved) — watch relay + compliance active."
