name: Nina (evolved) — watch engineering + security
on:
  push:
    branches: [ main ]
    paths:
      - "pages/apps/nina/**"
      - ".github/workflows/nina-evolved.yml"
  workflow_dispatch: {}
permissions: { contents: read }
concurrency: { group: nina-evolved-${ github.ref }, cancel-in-progress: true }
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Nina (evolved) — watch engineering + security active."
