export default { async fetch(req){ if(req.method!=='POST') return new Response('POST required',{status:405}); return new Response(JSON.stringify({ok:true}),{headers:{'content-type':'application/json'}}); } }