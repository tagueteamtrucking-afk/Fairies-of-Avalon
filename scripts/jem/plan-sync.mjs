import fs from 'node:fs'; import path from 'node:path'; const arg=process.argv[2]||'{}'; let sample={}; try{sample=JSON.parse(arg)}catch{}; const plan={generated:new Date().toISOString(),summary:'Example fitness plan',metrics:sample}; const outDir=path.join('pages','apps','jem','data'); fs.mkdirSync(outDir,{recursive:true}); fs.writeFileSync(path.join(outDir,'plan-latest.json'),JSON.stringify(plan,null,2)); console.log('Wrote plan-latest.json');