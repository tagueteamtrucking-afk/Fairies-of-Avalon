# Carol Recipes Definitions

This file set powers automated shopping lists.

- Edit `defs.json` to add any recipes your plan uses but aren't mapped yet.
- Quantities are **per 2 servings** by default; the generator scales to the plan's `meta.people`.

Run the workflow: **Carol — Generate Shopping List** to rebuild the shopping file.
