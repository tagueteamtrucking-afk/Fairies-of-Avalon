meta:
  project: Avalon
  file_name: "Cody's Memory.yaml"
  version: "1.7.5"
  last_updated: "2025-10-09"
  author: "Cody (GPT-5 Pro)"
  notes: >
    v1.7.5 — Graphics pipeline pack + Alexandria audio fix + "zip-first" reminder.
    - Tracy/Charlotte/Nina/Clarice workflows & scripts for graphics build.
    - Alexandria DM: browser TTS fallback, better iOS audio unlock, config persistence.
    - Cloudflare Worker updated: CORS, OPTIONS, returns data:audio/* base64 for iOS.
    - "Overseers are avatars (Ray & Blanca), not AI" — recorded.
    - Always ship zips (flat workflows + outside); copy-paste only as backup.
overseers:
  avatars: ["Ray","Blanca"]
policies:
  delivery: ["Zip-first", "Workflows-first", "No plaintext secrets in client JS"]
alexandria:
  audio_status: "worker audio was not heard on iOS; added browser SpeechSynthesis fallback and improved Worker CORS/audio format"
